events {}
http {
    server {
        listen 10000; # Porta padrão que o Render espera

        location / {
            proxy_pass http://localhost:8000;
        }

        location /grafana/ {
            proxy_pass http://localhost:3000/;
            proxy_set_header Host $host;
        }
    }
}